We use a min heap to store the elements. We traverse the array and insert the elements into our min heap. 

If the size of the min heap is greater than k then we remove the top element of the min heap to get rid of the lowest transaction and print the top element of the min heap.

